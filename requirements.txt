# Core Streamlit and related packages
streamlit==1.18.0
psycopg2-binary==2.9.3
altair_saver==0.5.0
altair_viewer==0.4.0
plotly==5.24.1  # Data visualization library for interactive graphs
pandas==1.5.3   # Data processing
numpy==1.23.4   # Numerical calculations
matplotlib==3.10.0 # Static visualizations
scikit_learn==1.6.0 # For machine learning (if used)

# Web scraping, parsing, and HTML processing
beautifulsoup4==4.12.3
lxml==4.4.1

# File handling (e.g., Excel and CSV processing)
openpyxl==3.1.5
xlrd==1.2.0
XlsxWriter==1.1.8
pyxlsb==1.0.10

# Data storage and database integration (if used)
SQLAlchemy==1.2.7
psycopg2-binary==2.9.3  # PostgreSQL integration (updated version)
mysql-connector-python==8.0.33 # MySQL connector

# Optional: For image processing
pillow==9.2.0  # PIL, for image processing
pytesseract==0.3.10  # OCR processing, if required

# Authentication/Authorization (optional, depending on use case)
pyjwt==2.8.0
keyring==25.5.0

# Miscellaneous utilities (optional based on specific needs)
requests==2.28.1
pyyaml==6.0.2
pytest==8.3.4 # For testing

# Development tools (for building dependencies)
Cython==3.0.11
setuptools==67.4.0
